// includes
#include			<behaviors.dtsi>
#include			<dt-bindings/zmk/keys.h>
#include			<dt-bindings/zmk/bt.h>
#include			<dt-bindings/zmk/outputs.h>

// layers
#define DEF			0
#define NUM 		1
#define NAV 		2
#define MIS			3

// default
#define DEF_R		&hm LGUI R
#define DEF_S		&hm LALT S
#define DEF_T		&hm LCTL T
#define DEF_H		&hm LSFT H
#define DEF_N		&hm LSFT N
#define DEF_A		&hm LCTL A
#define DEF_I		&hm LALT I
#define DEF_O		&hm LGUI O
#define DEF_COMMA   &mm_com_sem
#define DEF_DOT     &mm_dot_col

// navigation
#define NAV_SWI_LE  &mm_switch_left
#define NAV_SWI_RI  &mm_switch_right
#define NAV_UP      &mm_up_pgup
#define NAV_DOWN    &mm_down_pgdn
#define NAV_LEFT    &mm_left_home
#define NAV_RIGHT   &mm_right_end
#define NAV_SWAP    &hm LSFT LC(LA(TAB))
#define NAV_SEL_LE  &kp LC(LS(LEFT))
#define NAV_SEL_RI  &kp LS(LC(RIGHT))
#define NAV_SEL_UP  &kp LS(LC(UP_ARROW))
#define NAV_SEL_LI  &kp LA(LS(I))
#define NAV_TAB_LE  &kp LC(PG_UP)
#define NAV_TAB_RI  &kp LC(PG_DN)
#define NAV_PASTE   &hm RSFT LC(V)

// number
#define NUM_N0       &kp N0
#define NUM_N1       &kp N1
#define NUM_N2       &kp N2
#define NUM_N3       &kp N3
#define NUM_N4       &kp N4
#define NUM_N5       &kp N5
#define NUM_N6       &kp N6
#define NUM_N7       &kp N7
#define NUM_N8       &kp N8
#define NUM_N9       &kp N9

// miscellaneous

// referenced
#define DESK_L		&kp LC(LG(LEFT))
#define DESK_R		&kp LC(LG(RIGHT))
#define SLA_PREV	&kp LA(LS(UP))
#define SLA_NEXT	&kp LA(LS(DN))    

// other
#define XXXXXXXXXX	&none
#define ___XXXX___	&trans
#define __________  &trans

// keymap drawer
#define THUMB_LEFT  &kp F23
#define THUMB_RIGHT &kp F24